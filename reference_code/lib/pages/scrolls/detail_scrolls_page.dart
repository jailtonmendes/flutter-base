import 'package:flutter/material.dart';

class DetailScrollsPage extends StatelessWidget {
  const DetailScrollsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Detalhes dos times'),
      ),
      body: Container(),
    );
  }
}
